package org.opencv.android

import android.graphics.Bitmap
import org.opencv.core.Mat

/** PLACEHOLDER — not the real OpenCV. See /opencv/README.md. */
object Utils {
    fun bitmapToMat(bmp: Bitmap, mat: Mat) { /* no-op */ }
}
